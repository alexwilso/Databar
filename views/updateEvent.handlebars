<!-- Nav Bar -->
<div id="nav_bar">
    <ul>
        <li><a href="/">HOME</a></li>
        <li><a href="/employees">EMPLOYEES</a></li>
        <li><a href="/events">EVENTS</a></li>
        <li><a href="/menu">MENU</a></li>
        <li><a href="/inventory">INVENTORY</a></li>
        <li><a href="/jobs">JOBS</a></li>
    </ul>
</div>
<!-- Page Icon -->
<div>
    <img class="page_icon" src="/images/events.jpg">
</div>
    <h1>Edit Event</h1>

<fieldset>
{{#each results}}
<form id="editEmployees" action="/events" method="PUT">
    Event Name:
    <input id="event_name" type="text" name="event_name" value="{{this.event_name}}" required><br>
    Event Date:
    <input type="text" name="event_date" id="event_date" value="{{formatDate this.event_date}}" required><br>
    {{/each}}
    <!-- Loops through Employees object passed from index.js  -->
    Employee:
        <select id="employee_1" name="employee_1">
        <option value=""></option> <!-- NULL Option -->
        {{#each employees}}
        <option value={{this.employee_ID}}>{{this.first_name}} {{this.last_name}}</option>
        {{/each}}</select><br>
    <!-- Loops through Employees object passed from index.js  -->
    Employee:
        <select id="employee_2" name="employee_2">
        <option value=""></option> <!-- NULL Option -->
        {{#each employees}}
        <option value={{this.employee_ID}}>{{this.first_name}} {{this.last_name}}</option>
        {{/each}}</select><br>
    <!-- Loops through Employees object passed from index.js  -->
    Employee:
        <select id="employee_3" name="employee_3">
        <option value=""></option> <!-- NULL Option -->
        {{#each employees}}
        <option value={{this.employee_ID}}>{{this.first_name}} {{this.last_name}}</option>
        {{/each}}</select><br>
    <!-- Loops through Employees object passed from index.js  -->
    Employee:
        <select id="employee_4" name="employee_4">
        <option value=""></option> <!-- NULL Option -->
        {{#each employees}}
        <option value={{this.employee_ID}}>{{this.first_name}} {{this.last_name}}</option>
        {{/each}}</select><br>
      <!-- Loops through Employees object passed from index.js  -->
    Employee:
        <select id="employee_5" name="employee_5">
        <option value=""></option> <!-- NULL Option -->
        {{#each employees}}
        <option value={{this.employee_ID}}>{{this.first_name}} {{this.last_name}}</option>
        {{/each}}</select><br>
    {{#each results}}
    Guest Count:
    <input id="guest_count" type="integer" name="guest_count" value="{{this.guest_count}}" required><br>
    <span id="error"></span>
    {{/each}}
    Drink Special:
    <!-- Loops through drinks object passed from index.js  -->
    <select id="menu_item" name="menu_item">
        {{#each drinks}}
        <option value={{this.menu_item}}>{{this.drink_name}}</option>
        {{/each}}</select><br>
    <input type="submit" value="Submit">    
</form>
</fieldset>
<script src="{{this.events}}"></script>